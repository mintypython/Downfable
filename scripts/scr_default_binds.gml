///Used by KeyCont.  Checks what key is associated with what action
bind_right = vk_right;
bind_left = vk_left;
bind_up = vk_up;
bind_down = vk_down;
bind_primary = ord("Z");
bind_secondary = ord("X");
bind_pause = vk_escape;
